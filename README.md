# SLE Platform

Speech Learning Engine / Orion working repository.

## Goal of Sprint 0
Learner completes one Mission from start to finish using the new architecture.

## Run API locally
```bash
cd backend
python -m venv .venv
.venv\\Scripts\\activate   # Windows
pip install -r requirements.txt
copy .env.example .env
python app.py
```

Open: http://127.0.0.1:5000
